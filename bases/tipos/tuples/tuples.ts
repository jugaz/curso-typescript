
(() => {

    /* 
        TODO:El código anterior declara una variable llamada hero y la asigna a una matriz con dos
        elementos. El primer elemento es una cadena y el segundo elemento es un número.
    */
    const hero: [string, number, boolean] = ['wolverine', 100, true];
    hero[0] = 'Iromana';
    hero[1] = 50;
    hero[2] = false;
 

    console.log({ hero })

})()
